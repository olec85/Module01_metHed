'use strict'

function first(str) {
    if (!str) return str;

    return str[0].toUpperCase() + str.slice(1);

}

alert( first("привет Мир") );//непонимаю как сделать,получается только изменить первую букву подскажи чего не хватает





